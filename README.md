# KxAnyServer

Simple Knox license server

Edit the source code in the lib folder.

To create obfuscated classes with obfuscated strings, run the script:
`  build_encrypted.cmd`

Please use the result from the lib_obf folder
